{
  "name": "git-buddy",
  "type": "module",
  "version": "0.0.1-dev1",
  "bin": {
    "git-up": "./dist/git-up.js",
    "git-start": "./dist/git-start.js",
    "git-recent": "./dist/git-recent.js"
  },
  "files": [
    "./dist/*"
  ],
  "private": true,
  "scripts": {
    "dev": "bun build ./src/*.ts --outdir ./dist --splitting --target node --watch",
    "build": "bun build ./src/*.ts --outdir ./dist --splitting --target node"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "commander": "^14.0.1",
    "picocolors": "^1.1.1",
    "simple-git": "^3.28.0"
  }
}
